<div class="container">
  <img src="./coffee.jpg" alt="header image" id="imgHeader">
  <div class="centered">
    <h1>
      Saved Articles
    </h1>
    <br>
    <h5>Your Saved Articles</h5>
  </div>
</div>
<hr>

<div class="articlesHere">

  {{#each results}}
  <div>

    {{!-- <button data-id="{{_id}}" type="button" class="noteBtn" data-toggle="modal" data-target="#notes">Add a Note
    </button> --}}
    <button value="{{_id}}" class="deleteBtn" data-saved="{{saved}}">Delete Article</button>


    {{ title }}<br>
    <a href="https://www.nytimes.com{{link}}" target="_blank">Link to article</a>
    <br>
    {{summary}}
    <br>
    <hr>

  </div>

  {{/each}}
  {{!-- 
  <div class="modal" tabindex="-1" role="dialog" id="notes">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Add a Note</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <p>
            this should be in the modal body
          </p>

        </div>

        <input type="text" placeholder="Enter Text Here" class="noteTxt">
        <div class="modal-footer">
          <button data-save="saveNote" class="saveNoteBtn" type="button"
            class="btn btn-primary save-note but">Save</button>
        </div>
      </div>
    </div>
  </div> --}}


</div>